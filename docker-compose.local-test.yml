services:
  app:
    image: ghcr.io/dstmrk/scontrinozero:${APP_VERSION:-latest}
    container_name: scontrinozero-local-test
    env_file: .env
    ports:
      - "3000:3000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
