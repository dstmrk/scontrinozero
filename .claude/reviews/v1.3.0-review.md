# Code Review — v1.3.0 (delta v1.2.11..HEAD)

Scratch locale, non commitare. File aggiornato batch-by-batch.

## Stato batch

| #   | Area                                  | Stato   | Findings (B/C/M/m/n) |
| --- | ------------------------------------- | ------- | -------------------- |
| 1   | Export CSV (core pipeline)            | DONE    | 0/0/2/4/3            |
| 2   | Analytics backend                     | DONE    | 0/0/3/5/4            |
| 3   | Analytics frontend (UI + recharts)    | DONE    | 0/0/4/6/4            |
| 4   | Plans gating + Pro helpers            | DONE    | 0/0/3/9/4            |
| 5   | Profile / Settings (IVA + S3776)      | PENDING | —                    |
| 6   | Server / API regression surface       | PENDING | —                    |
| 7   | Lib / security / infra + test residui | PENDING | —                    |

Legenda severity: **B** blocker · **C** critical · **M** major · **m** minor · **n** nit.

## Resume protocol

1. Leggere questo file → trovare primo batch `PENDING`
2. Processare solo quel batch (leggere file, identificare issue)
3. Aggiungere findings alla sezione corrispondente + marcare batch come `DONE` nella tabella
4. Fermarsi e riportare all'utente

## Findings per batch

### Batch 1 — Export CSV (core pipeline)

File esaminati: `src/lib/csv.ts`, `src/lib/csv.test.ts`, `src/lib/receipts/csv-export.ts`, `src/lib/receipts/csv-export.test.ts`, `src/app/api/export/receipts/route.ts`, `src/app/api/export/receipts/route.test.ts`, `src/app/dashboard/storico/export-csv-button.tsx`, `src/app/dashboard/storico/export-csv-button.test.tsx`, `src/app/dashboard/storico/page.tsx`, `src/types/storico.ts`, e (referenziati) `src/components/storico/storico-client.tsx`, `src/lib/date-utils.ts`, `src/lib/receipts/document-lines.ts`.

#### Findings

1. **major** — `src/lib/receipts/csv-export.ts:90,29` — Mismatch colonna vs filtro. La query forza `kind = "SALE"` (riga 90) ma il CSV include la colonna `id_documento_annullato` (riga 29, popolata solo su righe `kind = "VOID"`). La colonna è quindi **sempre vuota** nell'export, anche per record `VOID_ACCEPTED` (che semanticamente sono SALE annullate). Per dare valore alla colonna serve o (a) rimuoverla, o (b) includere `kind = "VOID"` con kind-aware rendering, o (c) `LEFT JOIN` su `commercial_documents AS void_doc ON void_doc.voided_document_id = commercial_documents.id` per popolare la colonna sui record `VOID_ACCEPTED`. Aggiungere test che documenti il comportamento atteso. Bloccante per l'UX, non per la correttezza.

2. **major** — `src/lib/receipts/csv-export.ts:128-131` — OFFSET pagination cumulativa O(n²) per export grandi. Su un Pro con 50k scontrini gli ultimi batch fanno ~5000 row-scan ciascuno; il problema è esattamente quello chiamato out in PLAN.md backlog P2 ("Paginazione cursor-based su storico, export e Developer API"). Per i volumi attuali (alpha/beta) non è urgente, ma il pattern keyset è banale da introdurre subito mentre l'API è ancora interna: `where(and(...conds, lt(createdAt, cursor)))` con cursor estratto dall'ultimo row del batch precedente. Decidere se anticipare o segnalare nel CHANGELOG come known-limitation della v1.3.0.

3. **minor** — `src/lib/receipts/csv-export.ts:129` — OFFSET pagination senza tie-break secondario su `id`. Con scritture concorrenti durante un export (un nuovo SALE entra fra un batch e il successivo) lo stesso documento può comparire due volte o essere saltato perché lo shift di `createdAt` ripiega l'offset. Mitigare con `.orderBy(desc(createdAt), desc(id))`. Probabilità reale bassa (export raramente concorrenti con emissioni), ma costo zero.

4. **minor** — `src/app/api/export/receipts/route.ts:67-68` — Non-null assertion `user!.id` con commento giustificativo. Funziona ma è un code smell. Refactor preferibile: estrarre un early return `if (!user) return errorJson(401, "Non autenticato.")` PRIMA di `assertProPlan`, oppure rendere il tipo di ritorno di `assertProPlan` un discriminated union dove `ok: true` implica `user` non-null (rifletterebbe già l'invariante implementato).

5. **minor** — `src/app/api/export/receipts/route.ts:21-33` + `87-95` — Validazione data fatta due volte (regex Zod + `parseStrictIsoDateUtc`). Difesa-in-profondità OK ma il path "regex pass / strict-parse fail" produce un secondo errore 400 con messaggio identico — codice ridondante. Sostituibile con un singolo Zod `.refine(v => parseStrictIsoDateUtc(v) !== null, "...")`. Tenere se l'errore Zod è più chiaro all'utente, altrimenti consolidare.

6. **minor** — `src/app/dashboard/storico/page.tsx:39-43` — Calcolo default "ultimi 7 giorni" usa `today.toISOString().split('T')[0]` (UTC) invece del fuso fiscale Europe/Rome. Inconsistente con la fix recente `ddeefe4` che ha ancorato i bucket analytics a Europe/Rome. Tra mezzanotte UTC e l'1/2 di mattino italiano il default sarebbe "ieri" anziché "oggi". Usare `formatInTimeZone(new Date(), "Europe/Rome", "yyyy-MM-dd")` (date-fns-tz è già nelle deps per la fix analytics). Stessa logica replicata in `storico-client.tsx` per i fallback del DateRangePicker (verificare nel Batch successivo).

7. **nit** — `src/lib/csv.ts:61` — `FORMULA_LEADERS.has(str[0])`: per stringhe che iniziano con surrogate-pair `str[0]` restituisce il primo code-unit (half), non il code-point. Nessun rischio reale (i leader pericolosi sono tutti ASCII) ma per pulizia `str.codePointAt(0)` o un controllo `str.startsWith(c)` sui leader.

8. **nit** — `src/lib/receipts/csv-export.ts:42` — `publicRequest: unknown` perde tipizzazione del payload. Drizzle expone già `InferSelectModel`; un alias come `Pick<SelectCommercialDocument, 'publicRequest'>` o uno Zod schema dedicato chiarirebbe la shape. Compromesso accettabile, l'estrattore `extractPaymentMethod` è già difensivo.

9. **nit** — `src/lib/receipts/csv-export.test.ts:224-228` — `mockCalcDocTotal.mockImplementation` restituisce sempre `10` su entrambi i branch del ternario → il test non distingue gli output per documento. Il test passa ma non sta verificando la differenziazione. Cosmetico.

10. **nit** — `src/app/api/export/receipts/route.ts:39,122` — `buildFilename(from = "tutti", to = "tutti")` con magic string italiana ripetuta nel default. Estrarre in costante `FILENAME_ALL_FALLBACK = "tutti"` per coerenza e futura i18n. Trascurabile.

#### Note positive

- Escaping CSV RFC 4180 corretto, inclusa la protezione formula-injection (`=`, `+`, `-`, `@`, `\t`, `\r`) con apostrofo, e doppia-quote interna raddoppiata.
- BOM UTF-8 emesso prima dell'header (compat Excel italiano).
- Streaming via `ReadableStream` con `controller.error()` su errori DB: il client riceve un download troncato e percepisce il fallimento (no fake-success).
- Rate limit per-user (`csv:<userId>`) a 10/ora coerente con CLAUDE.md.
- Tutti i test hanno `expect()`, mock `RateLimiter` usa `function`, variabili `mock*` prefissate, validazione UUID irrilevante qui (boundary GET su query string strettamente validata).
- Pro gate enforcement server-side prima del rate limit: nessun bypass possibile dal client.
- `parseStrictIsoDateUtc` con round-trip check su year/month/day previene la normalizzazione JS di date impossibili (es. 2026-02-31).

#### Suggerimento prioritario per il tag v1.3.0

Findings 1 e 6 sono i due che potrebbero meritare un fix in v1.3.0 (UX confondente sulla colonna VOID + inconsistenza timezone storico vs analytics appena fixata). Le altre sono tutte aggiustamenti opzionali / accumulabili.

### Batch 2 — Analytics backend

File esaminati: `src/server/analytics-actions.ts`, `src/server/analytics-actions.test.ts`, `src/server/analytics-helpers.ts`, `src/app/dashboard/analytics/page.tsx`, e (referenziato) `src/lib/server-auth.ts` (solo per uso di `checkBusinessOwnership`).

#### Findings

1. **major** — `src/server/analytics-actions.ts:44-64` + `133-223` + `src/app/dashboard/analytics/page.tsx:51-55` — Amplificazione massiccia di auth/plan/DB per render: ogni delle 3 server action (`getAnalyticsKpis`, `getRevenueTimeseries`, `getPaymentBreakdown`) invoca `authorizePro` che fa `getAuthenticatedUser` + `checkBusinessOwnership` + `getPlan`, poi `fetchSaleDocsInRange` + `computeTotalsByDoc` (fetch lines). La pagina chiama le tre action in `Promise.all` e a sua volta esegue `getAuthenticatedUser` + `getPlan` per il gate UI. Conteggio totale per una sola visita: **4× `getPlan`** (1 in page + 3 in actions), **3× `checkBusinessOwnership`**, **3× docs query**, **3× lines query**, **4× Supabase auth call**. Per un Pro con range 90d ognuna delle 3 docs query scansiona migliaia di record. Fix consigliato: introdurre `getAnalyticsDataset(businessId, range)` (1 chiamata auth + 1 docs + 1 lines) e derivare i 3 output in memoria; oppure introdurre un `AnalyticsContext` cacheato per request via `react/cache`. Impatta latency percepita (priorità #1 del progetto) e costi DB.

2. **major** — `src/app/dashboard/analytics/page.tsx:57-63` — Silent error masking: `safeKpis = "error" in kpis ? ZERO_KPIS : kpis` (e analoghi su timeseries/breakdown) mostrano zeri senza alcun segnale visivo se una qualunque action torna `{error}`. La gate `getPlan` precedente rende lo scenario raro (downgrade nel mezzo di un render, race con auth refresh), ma se accade è invisibile e produce metriche fasulle viste dall'utente come reali. Minimo: `logger.warn` con `userId` e `businessId` e fallback UI esplicito ("Impossibile caricare i dati, riprova"). Decidere se davvero `ZERO_KPIS` è il fallback corretto.

3. **major** — `src/server/analytics-actions.ts` (tutto il file) — Nessun rate limit sulle server action analytics. Un Pro autenticato può spammare le 3 query (range 90d può scansionare migliaia di documenti) e degradare il DB. Le altre operazioni costose seguono già la convention CLAUDE.md (`emit:<userId>` 30/h, `void:<userId>` 10/h, `csv:<userId>` 10/h, `pdf:<ip>` 60/h). Aggiungere `analytics:<userId>` (suggerito 60/h come `pdf:`, oppure 120/h se le visite tipiche fanno polling). Istanziare il `RateLimiter` a livello modulo e applicarlo PRIMA di `fetchSaleDocsInRange`. Per-userId, non per-businessId, per ridurre l'attack surface.

4. **minor** — `src/server/analytics-actions.ts:66-73` — `validateRange` è dichiarata `async` ma non contiene `await`. Pattern coerente con `authorizePro` ma costa un microtask gratis a chiamata. Render sync: `function validateRange(range): { ok: true } | { ok: false; error: string }`.

5. **minor** — `src/server/analytics-actions.ts:101-112` — Query docs unbounded sul range: per 90d × N scontrini/giorno il risultato non ha `LIMIT`. PLAN.md backlog P2 cita esattamente `searchReceipts`/`exportUserData` ma non `analytics-actions` (la feature è nuova in v1.3.0): andrebbe aggiunto al backlog o mitigato con un safety `LIMIT 50_000`. In più: nessun `withStatementTimeout` wrap (altri server action usano `db-timeout.ts` per gli stessi pattern di scansione lunga). Aggiungerlo per non lasciare connessioni pinned su query degenere.

6. **minor** — `src/server/analytics-actions.ts:45-50` — `try { await getAuthenticatedUser() } catch { return { error: "Non autenticato." } }`. La catch-all converte qualsiasi errore (network blip Supabase, errore di parsing del JWT) in "Non autenticato" — fuorviante per il debugging e per il logging. Filtrare per il tipo d'errore atteso (e.g. `AuthRequiredError`) e rilanciare gli altri; oppure `logger.warn({err, businessId}, "authorizePro: unexpected error")` prima del return. Coerente con CLAUDE.md regola 23.

7. **minor** — `src/app/dashboard/analytics/page.tsx:23-55` — `getPlan` chiamato in page (riga 28) E poi 3× dentro le action via `authorizePro` = 4 chiamate per request. Mitigation: cache via `react/cache` su `getPlan` (vale anche per altre pagine che lo chiamano), oppure passare il `plan` già risolto alle action come parametro. Connesso al finding #1 di questo batch.

8. **minor** — `src/app/dashboard/analytics/page.tsx:51-55` — Range iniziale hardcoded a `"30d"`, `searchParams` non letti. Conseguenza: `/dashboard/analytics?range=90d` non è un deep link e al refresh il client perde lo stato del range scelto. Incoerente con `dashboard/storico/page.tsx` che legge `dal`/`al`/`stato` dai searchParams (vedi Batch 1). Allineare i pattern.

9. **nit** — `src/server/analytics-actions.ts:113` — `rows as DocRow[]` per la conditional spread di `selection` (la versione "con publicRequest" e quella "senza"). Eliminabile con un overload o due funzioni separate; cast è pragmatico ma sopprime un eventuale errore di shape.

10. **nit** — `src/server/analytics-helpers.ts:92-100` — Defensive `parts.find(...)?.value ?? "0"`: se Intl restituisse un set parti inatteso il fallback "0" produrrebbe noon UTC = giorno fiscale sbagliato. È un caso teorico, ma un `throw` esplicito sarebbe più safe (Intl non dovrebbe mai variare, e se variasse vogliamo saperlo).

11. **nit** — `src/server/analytics-helpers.ts:157-163` — `fillMissingDays` confronta `cursor < end` (string lex). Funziona perché `yyyy-MM-dd` è lex-sortable, ma non c'è un commento che documenti l'invariante (qualcuno potrebbe cambiare il formato a `dd-MM-yyyy` localizzato e rompere silenziosamente la loop terminale).

12. **nit** — `src/app/dashboard/analytics/page.tsx:44` — `<div />` come child obbligato di `ProFeatureGate`. Vuoto e senza semantica. Decidere: rendere `children` opzionale nel componente gate, oppure usare un `<></>` Fragment esplicito. Probabilmente da rifinire nel Batch 4 (revisione di ProFeatureGate).

#### Note positive

- Helpers timezone Europe/Rome ben isolati e separati dal pure path: `formatRomeDay`, `romeMidnightUtc`, `addCalendarDays`, `fillMissingDays` sono tutti puri, testabili senza mock di moduli pesanti. Aderente alla regola CLAUDE.md sulla testabilità.
- DST handled correttamente: `rangeToBounds` calcola noon UTC come ancora "DST-safe" prima di chiedere a Intl l'offset Rome. Test esistente cross-DST (`90d` ref maggio → from a febbraio in CET 23:00Z).
- `fetchSaleDocsInRange` evita di caricare `publicRequest` quando non serve (selezione condizionale), riducendo il payload per KPI/timeseries.
- Cents-based aggregation (`toCents` + `revenueCents`) evita drift IEEE-754 e si allinea al pattern di `computeReceiptTotals` in `document-lines.ts`.
- Test coprono i casi DST critici (00:30 Rome → bucket sul giorno fiscale corretto), `VOID_ACCEPTED` esclusi dal revenue, fallback "other" sui payment method, AOV con division-by-zero guard.
- Pure helpers esportati senza `"use server"`, importabili in test senza la machinery server action (commento esplicito in `analytics-helpers.ts:1-11`).

#### Suggerimento prioritario per il tag v1.3.0

Findings 1 e 3 sono i due da considerare prima del tag: il primo è un cost issue su performance percepita (principio #1 del progetto), il terzo è un buco di sicurezza dell'attack surface (mancanza di rate limit su path costoso). Il finding 2 si può differire se i log lo coprono. Le altre sono accumulabili.

### Batch 3 — Analytics frontend (UI + recharts)

File esaminati: `src/components/analytics/analytics-client.tsx`, `src/components/analytics/kpi-cards.tsx`, `src/components/analytics/kpi-cards.test.tsx`, `src/components/analytics/payment-breakdown.tsx`, `src/components/analytics/revenue-sparkline.tsx`, e (referenziato) `src/lib/utils.ts` (`formatCurrency`/`formatDate`).

#### Findings

1. **major** — `src/components/analytics/analytics-client.tsx:56-70` — Race condition su cambio range rapido. `handleRangeChange` lancia 3 server action in `Promise.all` dentro `startTransition`, senza cancellation né tracking della richiesta più recente. Se l'utente cambia range due volte velocemente (7d → 90d), il risultato della prima richiesta (più lenta) può arrivare dopo la seconda e sovrascrivere `kpis`/`timeseries`/`breakdown` con i dati sbagliati. React `useTransition` segnala solo `isPending`, non cancella. Soluzioni: (a) `useRef<AnalyticsRange>` per latest, ignorare i resolve se `nextRange !== latestRangeRef.current`; (b) `AbortController` (richiede che le server action accettino signal — non oggi); (c) **TanStack Query** (già nelle deps) con `queryKey: ['analytics', businessId, range]` gestisce nativamente latest-only + cache fra switch (7d → 30d → 7d torna istantaneo). Opzione (c) preferibile, coerente con tech stack documentato.

2. **major** — `src/components/analytics/revenue-sparkline.tsx:33,49-53` — Timezone bug client-side. `formatDate(value)` fa `new Date("2026-05-19").toLocaleDateString("it-IT")` SENZA `timeZone`. Il server bucket-ifica per giorno fiscale Europe/Rome (fix `ddeefe4`), ma `toLocaleDateString` usa la tz del browser. Per un browser configurato in tz != Europe/Rome (es. mobile in roaming oltre US) il `2026-05-19` (UTC midnight) viene re-interpretato come `2026-05-18T20:00-04:00` e mostrato come "18/05/2026". Inconsistente con la fix server. Fix: `formatDate(value, "numeric", "Europe/Rome")` (estendendo l'API) oppure trattare la stringa come calendar-date pura e splittarla (`const [y,m,d] = value.split("-")`).

3. **major** — `src/components/analytics/payment-breakdown.tsx` + `revenue-sparkline.tsx` — Mancano file di test. CLAUDE.md regola 7: "Every new file with logic must have a corresponding test file". I componenti hanno logica testabile: mapping label (`METHOD_LABELS[e.method] ?? e.method`), trasformazione `revenueCents / 100`, empty state. Decidere: (a) aggiungere `payment-breakdown.test.tsx` + `revenue-sparkline.test.tsx` con render minimi via Testing Library; (b) escludere recharts wrapper in `sonar.coverage.exclusions` + `vitest.config.ts` con commento "rendering SVG ricaths non testabile in jsdom" e estrarre le pure functions di mapping in un file separato testato.

4. **major** — `src/components/analytics/revenue-sparkline.tsx` (tutto il file) — Nessuno stato empty: se `data.length === 0` (Batch 2 finding #2 — silent error masking che propaga array vuoto, oppure davvero zero scontrini nel range) il chart renderizza un quadrato vuoto senza placeholder. `payment-breakdown.tsx:32-38` ha già il fallback "Nessuno scontrino nel periodo selezionato." → portarlo identico in sparkline per coerenza UX.

5. **minor** — `src/components/analytics/analytics-client.tsx:54,82-83` + `66-68` — `useTransition` mostra solo spinner; nessun banner errore. Se le action falliscono (vedi Batch 2 finding #2), l'utente vede: spinner → spinner sparisce → zeri/grafici vuoti. Aggiungere state `error: string | null` settato quando una action torna `{error}`, e renderizzare un banner non bloccante sopra le card.

6. **minor** — `src/components/analytics/payment-breakdown.tsx:68-72` + `revenue-sparkline.tsx:58-62` — `contentStyle.border: "1px solid rgba(0,0,0,0.08)"` hardcoded → tooltip illeggibile in dark mode (border quasi invisibile + background bianco default recharts su sfondo scuro). Usare `var(--border)` Tailwind o token theme-aware, e `var(--popover)` per il background. Coerente con il design system shadcn/ui.

7. **minor** — `src/components/analytics/payment-breakdown.tsx` + `revenue-sparkline.tsx` — A11y: recharts produce `<svg>` senza `aria-label`/`<title>`. Screen reader non descrive il chart. Aggiungere `aria-label="Grafico ricavi giornalieri"`/`"Ricavi per metodo di pagamento"` sul `<div>` wrapper, oppure renderizzare in `<figure>` con `<figcaption>` visivamente nascosto.

8. **minor** — `src/components/analytics/kpi-cards.test.tsx:30-36` — Assertion troppo loose: `expect(dashes.length).toBeGreaterThan(0)` passerebbe sia con 1 che con 3 dash. La logica corrente con `count === 0` produce dash su revenue + count + AOV (3 dash, non solo AOV come dice il commento). Sostituire con `expect(dashes).toHaveLength(3)` e correggere il commento. Distingue regressioni se uno dei 3 fallback venisse rimosso.

9. **minor** — `src/components/analytics/payment-breakdown.tsx:27` — `METHOD_LABELS[e.method] ?? e.method`: il fallback renderizza la stringa raw se il server emette un valore non normalizzato. Server `normalizePaymentMethod` già forza "PC"/"PE"/"other", quindi safe. Ma se il contratto si rompe (refactor server) il chart mostra il valore non localizzato sui ticks. Recharts SVG `<text>` non è injectable (XSS basso rischio), ma è bypass dell'invariante. Sostituire con `?? "Altro"` o aggiungere assertion lato build.

10. **nit** — `src/components/analytics/analytics-client.tsx:60-69` — Tre `setState` in serie dentro la transition; `kpis`/`timeseries`/`breakdown` possono essere temporaneamente inconsistenti (es. KPI nuovi + chart vecchi se React tarpasse i batch). Refactor verso `useReducer` rende atomico il set di tutti e 3 e semplifica anche la gestione dell'errore (vedi #5).

11. **nit** — `src/components/analytics/payment-breakdown.tsx:26-30` + `revenue-sparkline.tsx:19-22` — `chartData` ricomputato a ogni render. Per pochi punti irrilevante. `useMemo` sarebbe più ortodosso.

12. **nit** — `src/components/analytics/payment-breakdown.tsx:56` + `revenue-sparkline.tsx:41` — `${Math.round(value)}€` per Y-axis tick senza separatore migliaia. Per valori grandi (12345€) leggibilità ridotta. Cosmetic; coerente con sparkline-at-a-glance design.

13. **nit** — `src/components/analytics/kpi-cards.tsx` — `fromCents` e `formatCount` come helpers module-private. Se la conversione cents→euro viene usata anche da payment-breakdown e sparkline (entrambi fanno `e.revenueCents / 100` inline), centralizzare in `analytics-helpers.ts` come `fromCents()` (già esiste `toCents`, manca il dual).

14. **nit** — `package.json:50` — `recharts ^3.8.1` aggiunto in v1.3.0. ~100KB gzipped. Coerente con il principio "Leggeri sulle risorse"? Per 2 chart è giustificato; tree-shakeable; isolato a 2 client component. Considerare `dynamic import` per evitare di includerlo nel bundle JS del primo render della pagina (gli analytics sono usati solo dai Pro).

#### Note positive

- Tutti i componenti dichiarano `readonly` sui props (S6861 compliant).
- `KpiCards` ha test dedicato con copertura su currency formatting locale-tolerant (gestisce sia ICU full che Node minimo).
- Logica anti-NaN (`count === 0 ? "—" : formatCurrency(...)`) previene la regressione classica del division-by-zero rendering.
- Cents-based all'origine (server → client via `RevenuePoint.revenueCents`) coerente con la convenzione in `analytics-helpers.ts`. La conversione in euro avviene solo al confine UI.
- `MethodLabels` mappa correttamente i codici fiscali italiani ai termini user-friendly.
- `ResponsiveContainer` di recharts → layout fluido, niente width fissa che rompe su mobile (target principale del prodotto).
- Color palette via CSS variable `--primary` con fallback hsl → adattabile al theming.
- `useTransition` (anche se imperfetto sul racing) è il pattern React idiomatic per pending UI senza bloccare i controlli.

#### Suggerimento prioritario per il tag v1.3.0

Findings 1, 2 e 4 sono i tre da considerare prima del tag: race condition (UX confusa per Pro utenti reali), timezone bug client (anche se basso impatto in Italia), empty state mancante sulla sparkline (è il chart hero della dashboard). Finding 3 (mancanza test) è bloccante per quality gate SonarCloud se non si esclude esplicitamente. Le altre accumulabili.

### Batch 4 — Plans gating + Pro helpers

File esaminati: `src/lib/plans.ts`, `src/lib/plans-shared.ts`, `src/lib/plans.test.ts`, `src/components/billing/pro-feature-gate.tsx`, `src/components/billing/pro-feature-gate.test.tsx`, `src/lib/api-auth.ts`, `src/lib/api-auth.test.ts`.

#### Findings

1. **major** — `src/lib/plans.ts:85-89` — Catch-all error masking in `assertProPlan`. `try { info = await getPlan(authUserId) } catch { return 401 "Non autenticato." }`. Stesso pattern del Batch 2 finding #6: confonde 3 stati molto diversi (profile-not-found, DB timeout, network glitch) sotto un unico 401 misleading. Conseguenze: il client che riceve 401 fa logout/redirect a `/login` anche se l'utente è autenticato e il problema è transient. Fix: classificare l'errore — `instanceof DbTimeoutError` → 503, `instanceof ProfileNotFoundError` → 403, altrimenti rethrow.

2. **major** — `src/lib/api-auth.test.ts:309-325` — Test fire-and-forget rejection è racy. `await Promise.resolve()` non basta a garantire che il `.catch` del fire-and-forget abbia eseguito (servono almeno N microtask flushes, e Drizzle può inserire altri await interni). L'assertion `expect(mockLoggerWarn).toHaveBeenCalled()` può passare anche solo per side-effect di chiamate `logger.warn` precedenti nello stesso test. Pattern fragile. Refactor: esporre la promise interna al test (es. `_lastUsedUpdatePromise` per testing) oppure verificare l'effetto in un `it.todo` con `expect.poll`/`waitFor`.

3. **major** — `src/lib/api-auth.test.ts:89-93,140-144` — `vi.setSystemTime(NOW)` chiamato senza `vi.useFakeTimers()` precedente. Vitest documenta che `setSystemTime` richiede fake timers attivi; senza il setup, in alcune versioni è no-op silenzioso e `Date.now()` torna il clock reale. Il test "ritorna 401 se la key è scaduta" (linea 218) si basa su `expiresAt: new Date("2026-03-25T00:00:00Z")` < `NOW`. Se NOW non è effettivamente bloccato, il test passa solo perché il 2026-03-25 è nel passato rispetto al clock reale; al passare del tempo (post-2026-03-25) il test continuerebbe a passare per ragioni accidentali. Fix: aggiungere `vi.useFakeTimers({ now: NOW })` in `beforeEach` o `beforeAll`, e `vi.useRealTimers()` in `afterEach`.

4. **minor** — `src/lib/plans.ts:51-53,77-89` — Profile-not-found → 401 "Non autenticato.": tecnicamente l'utente È autenticato (token Supabase valido), manca solo il profilo (orphan auth user, race con `deleteAccount` parziale — vedi CLAUDE.md sezione "deleteAccount: delete auth user FIRST"). Più chiaro: 403 "Profilo non disponibile, contatta supporto" oppure trigger automatico di creazione profilo. Connesso al finding #1.

5. **minor** — `src/lib/plans.ts:56` — `profile.plan as Plan` cast senza runtime validation. Se il DB drift inserisce un valore non in `Plan` union (SQL manuale, migrazione errata, downgrade della enum constraint), il cast silenzia. Funzioni a valle (`canUsePro`, `canEmit`) ritornano default false → utente bloccato senza segnale. Wrap con un type guard `isPlan(value: string): value is Plan` e log `critical: true` su mismatch.

6. **minor** — `src/lib/plans.ts:9-22` — Re-export inline list fragile. Quando qualcuno aggiunge una costante a `plans-shared.ts` deve ricordare di aggiungerla anche qui, altrimenti i caller server non la vedono. Sostituibile con `export * from "@/lib/plans-shared"` + `export type * from "@/lib/plans-shared"` (Next 14+ supporta type re-export wildcard).

7. **minor** — `src/lib/plans-shared.ts:44-47` — `canEmit(plan, null)` ritorna true per developer plans. I developer plans dovrebbero emettere solo via API key, non da UI dashboard. Verificare che il gate UI sia altrove (probabilmente in `cassa` o emit-action) e documentare l'invariante.

8. **minor** — `src/lib/plans-shared.ts:111-120` — `canAddCatalogItem` developer_* sempre true. Verificare se il flow developer prevede catalog management (probabilmente no, sono API-only). Se irrilevante per developer, restituire false esplicitamente con commento "catalog UI not exposed to developer plans".

9. **minor** — `src/lib/plans.test.ts` — Mancano test per `getPlan` happy path su `pro`/`unlimited`/`developer_*`. Solo trial/starter testati. Importante per `developer_*` perché il cast `as Plan` su una stringa DB non validata potrebbe inserire varianti non rilevate (es. `developer_indi` per typo).

10. **minor** — `src/lib/plans.test.ts` — Manca test per `assertProPlan` con errore transient (DB timeout). Aggiungerebbe documentazione del comportamento corrente (catch-all → 401) e renderebbe esplicito il finding #1 quando si rifattorizza.

11. **minor** — `src/components/billing/pro-feature-gate.tsx` — `plan` prop stale dopo upgrade mid-sessione. Se un utente upgrade'a a Pro mentre la pagina è aperta, gli `<children>` restano nascosti finché non c'è un page reload. Mitigazione: `router.refresh()` dopo il return Stripe checkout (probabilmente già fatto in billing flow); documentare. Inverso: downgrade non blocca client-side ma il server enforce sui dati. Sicurezza OK, UX da rifinire.

12. **minor** — `src/components/billing/pro-feature-gate.tsx:45` — Hard-coded `href="/dashboard/settings#billing"`. Estrarre in costante `BILLING_SETTINGS_HREF` condivisa con `export-csv-button.tsx` (stesso link). Refactor da fare anche su altri upsell sparsi nel codebase.

13. **minor** — `src/lib/api-auth.ts:147-155` — `db.update(...).catch(...)` fire-and-forget senza `waitUntil`. Su Node runtime container è OK (event loop tiene la promise alive). Su runtime serverless/Edge il contesto può essere terminato prima del flush, perdendo silenziosamente l'update. Per Next.js c'è `import { waitUntil } from "next/server"` (`@vercel/functions`-style) che funziona su entrambi i runtime. Aggiungere quando si introduce un secondo target di deploy.

14. **minor** — `src/lib/api-auth.ts:117` — `throw err` su errori non-timeout. La route handler chiamante deve catturare e mappare a una response; ogni route lo fa in modo leggermente diverso → error envelope inconsistente. Già tracciato in PLAN.md backlog P3 "Error envelope uniforme API". Niente azione in scope v1.3.0.

15. **nit** — `src/lib/plans-shared.ts:83-86,99-103` — `Partial<Record<Plan, number>>` per `API_KEY_LIMITS` e `DEVELOPER_MONTHLY_LIMITS`: type accuracy migliorabile con `Pick<Record<Plan, number>, "pro">` o un sotto-tipo `DeveloperPlan`. Cosmetic, type-safety identica.

16. **nit** — `src/components/billing/pro-feature-gate.test.tsx` — Coverage parziale developer plans: solo `developer_indie` testato. `it.each([developer_indie, developer_business, developer_scale])` consoliderebbe.

17. **nit** — `src/lib/api-auth.test.ts` — `await import("./api-auth")` ripetuto in ogni `it`. Inefficiente (re-import + re-resolve mocks) ma nessun bug funzionale.

18. **nit** — `src/lib/plans.ts:7` — Commento "i client component MUST import" con "i" minuscolo. Probabilmente refuso (deve essere "I client component" o "client components MUST import"). Cosmetic.

#### Note positive

- Separazione client-safe (`plans-shared.ts`) vs server-only (`plans.ts`) è il pattern corretto per Next.js App Router. Il commento esplicito su perché serve evita refactor accidentali. Implementazione esemplare.
- `assertProPlan` ritorna un discriminated union (`{ok: true}` / `{ok: false, status, error}`) che le route handler possono usare senza try/catch — pattern coerente con `withStatementTimeout`/`AssertProPlanResult`.
- `api-auth.ts` segue rigorosamente CLAUDE.md: messaggi 401 unificati (no info leak su stato della key, dettaglio nei log), `withStatementTimeout` sul query path critico, 503 distinto da 401 (`code: "DB_TIMEOUT"`), throttle `last_used_at` con WHERE condizionale.
- Test coverage robusta su `plans-shared.ts`: 35+ casi che coprono boundary del trial, ogni plan transition (canUsePro, canUseApi, canEmit), e tutte le `_LIMITS` constants.
- `isApiKeyAuthError` come type guard discriminated explicit (`"status" in result`) — type narrowing affidabile.
- Pre-check formato chiave (`isValidApiKeyFormat`) prima del query DB — evita CPU/DB su brute-force traffic. Già aderente alla regola CLAUDE.md 29 (double-gate before expensive ops).

#### Suggerimento prioritario per il tag v1.3.0

Findings 1 (catch-all in `assertProPlan`) e 3 (`vi.setSystemTime` senza fake timers) meritano un fix in v1.3.0: il primo per non maskerare problemi transient come "Non autenticato" all'utente; il secondo perché un test che non blocca il clock è una bomba a tempo (passa fino al 2026-03-25 e poi inizia a comportarsi diversamente). Il finding 2 (test racy) è meno urgente ma è già brittle. Gli altri sono accumulabili.

### Batch 5 — Profile / Settings (IVA + S3776)

_pending_

### Batch 6 — Server / API regression surface

_pending_

### Batch 7 — Lib / security / infra + test residui

_pending_

## Action plan (compilato a chiusura)

_da compilare quando tutti i batch sono DONE: aggregato findings, decisione tag v1.3.0 vs PR fix_
